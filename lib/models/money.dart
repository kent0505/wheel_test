class Money {
  Money({
    required this.money,
    required this.last,
    required this.onboard,
    required this.results,
  });

  double money;
  double last;
  bool onboard;
  List<String> results;
}
